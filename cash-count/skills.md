---

### 2. `skills.md`
This file is a great addition to a portfolio repository. It explicitly translates the code you wrote into the industry-standard skills recruiters look for.

```markdown
# Technical Skills Demonstrated

Building the CashCount System demonstrates proficiency in the following areas:

* **Backend Development (Java/Spring Boot)**
  * Implemented a Model-View-Controller (MVC) architecture to separate business logic, data presentation, and routing.
  * Managed dependency injection and application context using Spring Boot.
* **Database Management (Spring Data JPA / H2)**
  * Designed a normalized relational database schema handling Users, Budgets, and Transactions.
  * Mapped Java Entity classes to database tables using JPA annotations (`@Entity`, `@OneToMany`, `@ManyToOne`).
  * Utilized Spring Data Repositories for streamlined CRUD operations.
* **Frontend Engineering (Thymeleaf / Tailwind CSS)**
  * Developed a responsive, mobile-first user interface using Tailwind CSS and Flowbite components.
  * Bound server-side Java data dynamically to HTML templates using Thymeleaf syntax (`th:text`, `th:each`).
* **Software Design & Planning**
  * Architected system flows using standard UML Use Case and Entity-Relationship Diagrams.